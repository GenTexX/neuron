{
  "name": "@neuron/games",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DOM-freie Spiellogik: Generatoren, Judges, Scorer, Registry",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./ranked-configs.json": "./ranked-configs.json"
  },
  "scripts": {
    "test": "vitest run",
    "check": "tsc --noEmit -p tsconfig.json",
    "fixtures": "vitest run --config vitest.fixtures.config.ts"
  },
  "dependencies": {
    "@neuron/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
